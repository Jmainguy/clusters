apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shoreline
  namespace: argocd
spec:
  project: default
  source:
    chart: jmainguy/helm-webapp
    repoURL: zot.soh.re
    targetRevision: 4.2.4
    helm:
      values: |
        name: shoreline
        replicaCount: 1
        service:
          type: ClusterIP
          port: 80
          targetPort: 8080
          name: shoreline
        virtualService:
          hosts:
            - "shoreline.soh.re"
        image:
          repository: zot.soh.re
          name: jmainguy/shoreline
          tag: "v1.0.1"
          pullPolicy: IfNotPresent

  destination:
    name: k3s.soh.re
    namespace: shoreline
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
